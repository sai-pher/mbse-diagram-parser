# Runtime dependencies
# (none for core parser)

# Optional: Diagram generation
# drawpyo>=0.2.0  # Uncomment for diagram generation support

# Development dependencies
pytest>=7.0
pytest-cov>=4.0
ruff>=0.4
black>=24.0
mkdocs>=1.5
mkdocs-material>=9.0
